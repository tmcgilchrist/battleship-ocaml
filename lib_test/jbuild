(jbuild_version 1)

(executables
 ((libraries (battleship oUnit core))
  (modules (board_test))
  (names (board_test))))

(alias
 ((name runtest)
  (package battleship)
  (deps (board_test.exe))
  (action (run ${<}))))
